apiVersion: v1
kind: Pod
metadata:
  name: pod-nodepod-service
  namespace: roboshop
  labels: 
    project: roboshop
    component: frontend
    environment: dev
    purpose: node-pod
spec:
  containers:
  - name: nginx
    image: nginx

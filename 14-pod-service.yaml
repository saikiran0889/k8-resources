apiVersion: v1
king: Pod
metadata:
  name: pod-service
  namespace: roboshop
  labels: 
    project: roboshop
    component: frontend
    environment: dev
spec:
  containers:
  - name: nginx
    image: nginx